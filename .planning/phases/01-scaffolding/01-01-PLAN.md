---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - .gitignore
  - eslint.config.js
  - .prettierrc
  - .husky/pre-commit
  - .lintstagedrc
  - src/lib/utils.ts
  - components.json
  - tailwind.config.ts
  - src/index.css
  - postcss.config.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev starts Vite 7 dev server rendering a React 19 component"
    - "npm run build produces production bundle with zero errors and zero warnings"
    - "ESLint 9 flat config with strictTypeChecked catches type errors"
    - "Prettier formats code on pre-commit via Husky + lint-staged"
    - "shadcn/ui is initialized and ready for component installation"
  artifacts:
    - path: "package.json"
      provides: "All project dependencies"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite 7 configuration with roslib CJS workaround"
      contains: "optimizeDeps"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config with strict TypeScript"
      contains: "strictTypeChecked"
    - path: "src/App.tsx"
      provides: "Root React 19 component"
      contains: "function App"
    - path: "index.html"
      provides: "Vite entry point"
      contains: "src/main.tsx"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script tag"
      pattern: "src/main.tsx"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React.createRoot render"
      pattern: "createRoot"
    - from: "vite.config.ts"
      to: "node_modules/roslib"
      via: "optimizeDeps.include"
      pattern: "roslib"
---

<objective>
Clean slate the v3 Next.js codebase and create a fresh Vite 7 + React 19 project with all dependencies, linting, formatting, and shadcn/ui initialized.

Purpose: Establish the verified build foundation that all subsequent phases build on. Every dependency must be installed and the toolchain must be proven working before any feature code.
Output: A working Vite 7 project with React 19, TypeScript 5.9, all dependencies installed, ESLint 9 strict config, Prettier, Husky pre-commit hooks, and shadcn/ui initialized.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/1-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean slate and create Vite 7 + React 19 project with all dependencies</name>
  <files>
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json,
    vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts,
    .gitignore
  </files>
  <read_first>
    .gitignore (current state -- needs updating from Next.js to Vite entries),
    CLAUDE.md (code conventions, architecture, gotchas -- especially roslib CommonJS note)
  </read_first>
  <action>
**Step 1: Clean slate (per D-01, D-02)**

Delete all v3 artifacts. Keep ONLY these:
- `.planning/` directory (entire tree)
- `CLAUDE.md`
- `docs/` directory
- `.git/` directory
- `.gitignore` (will be updated)

Delete specifically:
- `app/`, `components/`, `hooks/`, `lib/`, `__tests__/`, `e2e/`, `dist/`, `.husky/`, `.github/`
- `package.json`, `package-lock.json`, `node_modules/`
- `eslint.config.mjs`, `jest.config.js`, `components.json`, `.coveragerc`, `LICENSE`
- `tsconfig.json`, `next.config.*`, `tailwind.config.*`, `postcss.config.*`
- All `audit-*.png` and `rethink-*.png` files (per D-02)
- `.playwright-mcp/` directory

Use `rm -rf` for directories, `rm -f` for files. Verify with `ls` that only `.planning/`, `CLAUDE.md`, `docs/`, `.git/`, `.gitignore` remain.

**Step 2: Create Vite 7 project**

Run `npm create vite@latest . -- --template react-ts` (use `.` for current directory, overwrite existing files).

If the scaffolder doesn't support `--template` in current dir, manually create the files:

`package.json`:
```json
{
  "name": "robot-telemetry-dashboard",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest",
    "prepare": "husky"
  }
}
```

**Step 3: Install dependencies**

Core (exact versions where specified):
```bash
npm install react@19 react-dom@19
npm install roslib@2.0.1
npm install recharts react-is
npm install react-grid-layout
npm install d3
npm install rxjs
npm install zustand
npm install react-router-dom
```

Dev dependencies:
```bash
npm install -D typescript@~5.9 @types/react @types/react-dom
npm install -D vite@^7 @vitejs/plugin-react
npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom
npm install -D @playwright/test
npm install -D @types/d3 @types/react-grid-layout
```

Note: If `react-is` version conflicts with Recharts, add an `overrides` field in package.json:
```json
"overrides": {
  "react-is": "$react-is"
}
```
This forces Recharts to use the installed react-is version compatible with React 19.

**Step 4: Configure Vite**

`vite.config.ts`:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'node:path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  optimizeDeps: {
    include: ['roslib'],
  },
});
```

The `optimizeDeps.include: ['roslib']` is MANDATORY -- roslib is CommonJS and Vite needs to pre-bundle it (per CLAUDE.md gotcha).

**Step 5: Configure TypeScript**

`tsconfig.json`:
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

`tsconfig.app.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "forceConsistentCasingInFileNames": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

`tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts"]
}
```

**Step 6: Create minimal App**

`src/main.tsx`:
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';

const rootElement = document.getElementById('root');
if (!rootElement) throw new Error('Root element not found');

createRoot(rootElement).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`src/App.tsx`:
```tsx
export function App() {
  return (
    <div id="app-root">
      <h1>Robot Telemetry Dashboard</h1>
      <p>v4 — Scaffolding complete</p>
    </div>
  );
}
```

`src/vite-env.d.ts`:
```typescript
/// <reference types="vite/client" />
```

`index.html`:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Robot Telemetry Dashboard</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 7: Update .gitignore for Vite**

Replace the current .gitignore contents with:
```
# dependencies
node_modules/

# build output
dist/

# environment
.env*

# editor
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*

# typescript
*.tsbuildinfo

# testing
coverage/
test-results/
playwright-report/

# claude
.claude/

# MCP config (may contain API keys)
.mcp.json

# Playwright MCP artifacts
.playwright-mcp/

# Audit screenshots (not in .planning/)
audit-*.png
rethink-*.png
screenshots/
```

**Step 8: Verify**

Run `npm install` to generate lockfile. Then run `npx tsc -b --noEmit` and `npx vite build` to verify the project compiles.
  </action>
  <verify>
    <automated>cd /Users/ragglesoft/Desktop/software-dev/robot-telemetry-dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Only `.planning/`, `CLAUDE.md`, `docs/`, `.git/`, `.gitignore`, and new Vite files exist at root (no `app/`, `components/`, `hooks/`, `__tests__/`, `audit-*.png`)
    - `package.json` contains `"react": "^19"` or `"react@19"`
    - `package.json` contains `"roslib"`, `"recharts"`, `"d3"`, `"react-grid-layout"`, `"rxjs"`, `"zustand"`, `"react-router-dom"`
    - `package.json` contains `"vite"` in devDependencies
    - `package.json` scripts has `"dev": "vite"` and `"build": "tsc -b && vite build"`
    - `vite.config.ts` contains `optimizeDeps` with `include: ['roslib']`
    - `tsconfig.app.json` contains `"strict": true` and `"noUncheckedIndexedAccess": true`
    - `tsconfig.app.json` contains `"@/*": ["./src/*"]` in paths
    - `index.html` contains `src/main.tsx`
    - `src/main.tsx` contains `createRoot`
    - `src/App.tsx` contains `export function App`
    - `.gitignore` contains `dist/` and does NOT contain `/.next/`
    - `npm run build` exits with code 0, zero errors, zero warnings
  </acceptance_criteria>
  <done>Clean Vite 7 + React 19 project builds successfully with all dependencies installed. No v3 artifacts remain.</done>
</task>

<task type="auto">
  <name>Task 2: ESLint 9 + Prettier + Husky + lint-staged + shadcn/ui init</name>
  <files>
    eslint.config.js, .prettierrc, .husky/pre-commit, .lintstagedrc,
    components.json, tailwind.config.ts, postcss.config.js,
    src/lib/utils.ts, src/index.css
  </files>
  <read_first>
    package.json (just created -- need current dependency list),
    tsconfig.app.json (ESLint needs projectService alignment),
    CLAUDE.md (ESLint rules, code conventions, two font weights: 400/600)
  </read_first>
  <action>
**Step 1: Install ESLint + Prettier dev dependencies (per D-05 through D-09)**

```bash
npm install -D eslint@^9 @eslint/js typescript-eslint
npm install -D eslint-plugin-react-hooks eslint-plugin-react-refresh
npm install -D prettier eslint-config-prettier
npm install -D husky lint-staged
```

**Step 2: ESLint 9 flat config (per D-05, D-06, D-07)**

Create `eslint.config.js` (NOT `.mjs` -- per D-05, use `defineConfig`):
```javascript
import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';

export default tseslint.config(
  { ignores: ['dist', 'node_modules', '.planning'] },
  js.configs.recommended,
  ...tseslint.configs.strictTypeChecked,
  ...tseslint.configs.stylisticTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
  {
    plugins: {
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': [
        'warn',
        { allowConstantExport: true },
      ],
    },
  },
);
```

Key points:
- Uses `tseslint.config()` wrapper (the `defineConfig` equivalent for typescript-eslint)
- `strictTypeChecked` + `stylisticTypeChecked` presets (per D-06)
- `projectService: true` (per D-07) -- NOT `project: ['./tsconfig.app.json']`
- Ignores `dist`, `node_modules`, `.planning`

**Step 3: Prettier config (per D-08)**

Create `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 80,
  "tabWidth": 2
}
```

**Step 4: Husky + lint-staged (per D-03, D-08)**

Run `npx husky init` to create `.husky/` directory.

Update `.husky/pre-commit`:
```bash
npx lint-staged
```

Create `.lintstagedrc`:
```json
{
  "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
  "*.{js,json,md,css}": ["prettier --write"]
}
```

**Step 5: shadcn/ui init (per D-04)**

Install Tailwind CSS + dependencies for Vite:
```bash
npm install -D tailwindcss @tailwindcss/vite
```

Run `npx shadcn@latest init` with these selections:
- Style: New York
- Base color: Neutral (will be overridden in Phase 2 with defense-contractor tokens)
- CSS variables: Yes

If interactive prompts are an issue, create the config files manually:

`components.json`:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

Note: `"rsc": false` because this is Vite, not Next.js.

`src/lib/utils.ts`:
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Install shadcn utility deps:
```bash
npm install clsx tailwind-merge
npm install -D @types/node
```

`postcss.config.js`:
```javascript
export default {};
```

Note: With `@tailwindcss/vite` plugin, PostCSS is not needed for Tailwind processing, but shadcn expects the file to exist. Keep it minimal.

Update `vite.config.ts` to add the Tailwind plugin:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'node:path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  optimizeDeps: {
    include: ['roslib'],
  },
});
```

`src/index.css` (Tailwind v4 with `@tailwindcss/vite`):
```css
@import 'tailwindcss';
```

Update `src/main.tsx` to import the CSS:
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';
import './index.css';

const rootElement = document.getElementById('root');
if (!rootElement) throw new Error('Root element not found');

createRoot(rootElement).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

**Step 6: Verify toolchain**

Run `npm run lint` -- should pass with zero errors.
Run `npx prettier --check src/` -- should report all files formatted.
Run `npm run build` -- should still pass.
  </action>
  <verify>
    <automated>cd /Users/ragglesoft/Desktop/software-dev/robot-telemetry-dashboard && npm run lint && npx prettier --check "src/**/*.{ts,tsx}" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` exists (NOT `.mjs`) and contains `strictTypeChecked` and `stylisticTypeChecked`
    - `eslint.config.js` contains `projectService: true`
    - `eslint.config.js` contains `react-hooks` and `react-refresh` plugins
    - `.prettierrc` exists and contains `"singleQuote": true`
    - `.husky/pre-commit` exists and contains `lint-staged`
    - `.lintstagedrc` exists and contains `"*.{ts,tsx}"` entry
    - `components.json` exists and contains `"rsc": false`
    - `src/lib/utils.ts` exists and contains `export function cn`
    - `src/index.css` exists and contains `@import 'tailwindcss'`
    - `vite.config.ts` contains `tailwindcss()` in plugins array
    - `vite.config.ts` still contains `optimizeDeps` with `roslib`
    - `npm run lint` exits 0 with no errors
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>ESLint 9 strict config, Prettier, Husky pre-commit hooks, and shadcn/ui are all configured and verified working. The full toolchain (lint + format + build) passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` produces zero errors, zero warnings
2. `npm run lint` passes with zero errors
3. `npx prettier --check "src/**/*.{ts,tsx}"` reports all formatted
4. No v3 artifacts remain (no `app/`, `components/`, `__tests__/`, `audit-*.png`)
5. `node_modules/roslib` exists (installed successfully)
</verification>

<success_criteria>
- Vite 7 dev server starts and renders React 19 component
- Production build succeeds with zero errors/warnings
- ESLint 9 with strictTypeChecked catches type-level errors
- Prettier + Husky + lint-staged enforce formatting on commit
- shadcn/ui initialized and ready for Phase 2 design tokens
- All dependencies installed: react 19, roslib, recharts, d3, react-grid-layout, rxjs, zustand, react-router-dom
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-01-SUMMARY.md`
</output>
