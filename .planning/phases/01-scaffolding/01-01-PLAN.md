---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/vite-env.d.ts
  - src/index.css
  - src/lib/utils.ts
  - .gitignore
  - eslint.config.js
  - .prettierrc
  - .husky/pre-commit
  - components.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "npm run dev starts Vite 7 dev server rendering a React 19 component"
    - "npm run build produces production bundle with zero errors and zero warnings"
    - "npm run lint passes with zero errors using ESLint 9 strictTypeChecked"
    - "Prettier formats code on pre-commit via Husky + lint-staged"
    - "shadcn/ui is initialized with Tailwind CSS v4 Vite plugin (no postcss.config.js, no tailwind.config.js)"
  artifacts:
    - path: "package.json"
      provides: "All project dependencies"
      contains: "react"
    - path: "vite.config.ts"
      provides: "Vite 7 configuration with React and Tailwind v4 plugins"
      contains: "tailwindcss"
    - path: "eslint.config.js"
      provides: "ESLint 9 flat config with strict TypeScript"
      contains: "strictTypeChecked"
    - path: "src/App.tsx"
      provides: "Root React 19 component"
      contains: "export function App"
    - path: "index.html"
      provides: "Vite entry point"
      contains: "src/main.tsx"
  key_links:
    - from: "index.html"
      to: "src/main.tsx"
      via: "script tag"
      pattern: "src/main.tsx"
    - from: "src/main.tsx"
      to: "src/App.tsx"
      via: "React.createRoot render"
      pattern: "createRoot"
    - from: "vite.config.ts"
      to: "@tailwindcss/vite"
      via: "Vite plugin"
      pattern: "tailwindcss"
---

<objective>
Clean slate the v3 Next.js codebase and create a fresh Vite 7 + React 19 project with all dependencies, ESLint 9 strict config, Prettier, Husky pre-commit hooks, and shadcn/ui initialized with Tailwind CSS v4.

Purpose: Establish the verified build foundation that all subsequent phases build on. Every dependency must be installed and the full toolchain (lint + type-check + build) proven working.
Output: A working Vite 7 project with React 19, TypeScript 5.9, all dependencies installed, ESLint 9 strict config, Prettier, Husky pre-commit hooks, and shadcn/ui initialized via Tailwind v4 Vite plugin.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding/1-CONTEXT.md
@.planning/phases/01-scaffolding/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean slate and create Vite 7 + React 19 project with all dependencies</name>
  <files>
    package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json,
    vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/vite-env.d.ts,
    src/index.css, .gitignore
  </files>
  <read_first>
    .gitignore (current state -- needs updating from Next.js to Vite entries),
    CLAUDE.md (code conventions, architecture, gotchas)
  </read_first>
  <action>
**Step 1: Clean slate (per D-01, D-02)**

Delete all v3 artifacts. Keep ONLY these:
- `.planning/` directory (entire tree)
- `CLAUDE.md`
- `docs/` directory
- `.git/` directory
- `.gitignore` (will be overwritten)

Delete specifically (use `rm -rf` for dirs, `rm -f` for files):
- All directories: `app/`, `components/`, `hooks/`, `lib/`, `__tests__/`, `e2e/`, `dist/`, `.husky/`, `.github/`, `node_modules/`, `.playwright-mcp/`, `src/`
- All config files: `package.json`, `package-lock.json`, `eslint.config.mjs`, `eslint.config.js`, `jest.config.js`, `components.json`, `.coveragerc`, `LICENSE`, `tsconfig.json`, `tsconfig.*.json`, `next.config.*`, `tailwind.config.*`, `postcss.config.*`, `next-env.d.ts`
- All v3 screenshots: `audit-*.png`, `rethink-*.png` (per D-02)

Verify with `ls -a` that only `.planning/`, `CLAUDE.md`, `docs/`, `.git/`, `.gitignore` remain at root.

**Step 2: Create package.json and install all dependencies**

Create `package.json`:
```json
{
  "name": "robot-telemetry-dashboard",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "test": "vitest",
    "prepare": "husky"
  }
}
```

Install core dependencies:
```bash
npm install react@^19.0.0 react-dom@^19.0.0 react-is@^19.0.0
npm install roslib@^2.0.1 rxjs@^7.8.0 zustand@^5.0.0
npm install recharts@^3.8.0 d3@^7.9.0 react-grid-layout@^2.2.2
npm install react-router-dom@^7.0.0
npm install clsx tailwind-merge
```

IMPORTANT: `react-is@^19.0.0` is installed as a regular dependency because recharts 3.x lists it as a peerDependency. No npm overrides needed.

Install dev dependencies:
```bash
npm install -D vite@^7.2.2 @vitejs/plugin-react typescript@~5.9.3
npm install -D @types/react @types/react-dom @types/d3 @types/node
npm install -D tailwindcss@^4.0.0 @tailwindcss/vite tw-animate-css
npm install -D eslint@^9.0.0 @eslint/js typescript-eslint eslint-plugin-react-hooks eslint-plugin-react-refresh eslint-config-prettier
npm install -D prettier husky lint-staged
npm install -D vitest@^4.1.0 @testing-library/react @testing-library/jest-dom jsdom
npm install -D @playwright/test@^1.58.0
```

DO NOT install `@types/react-grid-layout` -- react-grid-layout 2.x ships its own TypeScript types.

**Step 3: Configure Vite**

`vite.config.ts`:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'node:path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

IMPORTANT: Do NOT add `optimizeDeps.include: ['roslib']`. roslib 2.0.1 is pure ESM -- the CJS workaround from v1/v2 is no longer needed.

**Step 4: Configure TypeScript**

`tsconfig.json`:
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

`tsconfig.app.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "forceConsistentCasingInFileNames": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

`tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts", "vitest.config.ts", "eslint.config.js"]
}
```

Note: `tsconfig.node.json` includes `vitest.config.ts` and `eslint.config.js` so the ESLint `projectService: true` setting covers config files (avoids Pitfall 4 from research).

**Step 5: Create minimal App**

`src/main.tsx`:
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';
import './index.css';

const rootElement = document.getElementById('root');
if (!rootElement) throw new Error('Root element not found');

createRoot(rootElement).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`src/App.tsx`:
```tsx
export function App() {
  return (
    <div id="app-root">
      <h1>Robot Telemetry Dashboard</h1>
      <p>v4 -- Scaffolding complete</p>
    </div>
  );
}
```

`src/vite-env.d.ts`:
```typescript
/// <reference types="vite/client" />
```

`src/index.css` (Tailwind v4 -- CSS-first, no JS config):
```css
@import "tailwindcss";
@import "tw-animate-css";
```

No `tailwind.config.js` or `postcss.config.js` -- Tailwind v4 uses the Vite plugin and CSS `@theme` directive.

`index.html`:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Robot Telemetry Dashboard</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 6: Update .gitignore for Vite**

Overwrite `.gitignore`:
```
# dependencies
node_modules/

# build output
dist/

# environment
.env*

# editor
.DS_Store
*.pem

# debug logs
npm-debug.log*

# typescript
*.tsbuildinfo

# testing
coverage/
test-results/
playwright-report/

# claude
.claude/

# MCP config (may contain API keys)
.mcp.json

# Playwright MCP artifacts
.playwright-mcp/
```

**Step 7: Verify**

Run `npm run build` -- must exit 0 with zero errors and zero warnings.
  </action>
  <verify>
    <automated>cd /Users/ragglesoft/Desktop/software-dev/robot-telemetry-dashboard && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Only `.planning/`, `CLAUDE.md`, `docs/`, `.git/`, `.gitignore`, and new Vite files exist at root (no `app/`, `components/`, `hooks/`, `__tests__/`, `audit-*.png`, `next.config.*`)
    - `package.json` contains `"react"` and `"react-dom"` in dependencies
    - `package.json` contains `"react-is"` in dependencies (NOT in overrides)
    - `package.json` contains `"roslib"`, `"recharts"`, `"d3"`, `"react-grid-layout"`, `"rxjs"`, `"zustand"`, `"react-router-dom"` in dependencies
    - `package.json` contains `"vite"` in devDependencies
    - `package.json` does NOT contain `@types/react-grid-layout` in devDependencies
    - `package.json` scripts has `"dev": "vite"` and `"build": "tsc -b && vite build"`
    - `vite.config.ts` contains `tailwindcss()` in plugins array
    - `vite.config.ts` does NOT contain `optimizeDeps` (roslib 2.x is pure ESM)
    - `tsconfig.app.json` contains `"strict": true` and `"noUncheckedIndexedAccess": true`
    - `tsconfig.app.json` contains `"@/*": ["./src/*"]` in paths
    - `tsconfig.node.json` include array contains `"vitest.config.ts"` and `"eslint.config.js"`
    - `index.html` contains `src/main.tsx`
    - `src/main.tsx` contains `createRoot` and `import './index.css'`
    - `src/App.tsx` contains `export function App`
    - `src/index.css` contains `@import "tailwindcss"` and `@import "tw-animate-css"`
    - No `tailwind.config.js`, `tailwind.config.ts`, or `postcss.config.js` exists
    - `.gitignore` contains `dist/` and does NOT contain `/.next/`
    - `npm run build` exits with code 0, zero errors, zero warnings
  </acceptance_criteria>
  <done>Clean Vite 7 + React 19 project builds successfully with all dependencies installed. No v3 artifacts remain. Tailwind v4 integrated via Vite plugin (no legacy config files).</done>
</task>

<task type="auto">
  <name>Task 2: ESLint 9 strict config + Prettier + Husky + lint-staged + shadcn/ui init</name>
  <files>
    eslint.config.js, .prettierrc, .husky/pre-commit, src/lib/utils.ts, components.json
  </files>
  <read_first>
    package.json (verify eslint, prettier, husky, lint-staged are installed),
    tsconfig.app.json (ESLint projectService needs alignment),
    tsconfig.node.json (must include eslint.config.js),
    vite.config.ts (verify Tailwind plugin present for shadcn),
    CLAUDE.md (code conventions, ESLint rules)
  </read_first>
  <action>
**Step 1: Create ESLint 9 flat config (per D-05, D-06, D-07, D-09)**

`eslint.config.js`:
```javascript
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';
import eslintConfigPrettier from 'eslint-config-prettier';

export default tseslint.config(
  { ignores: ['dist/', 'node_modules/', '.planning/', 'e2e/'] },
  eslint.configs.recommended,
  ...tseslint.configs.strictTypeChecked,
  ...tseslint.configs.stylisticTypeChecked,
  {
    languageOptions: {
      parserOptions: {
        projectService: true,
        tsconfigRootDir: import.meta.dirname,
      },
    },
  },
  {
    plugins: {
      'react-hooks': reactHooks,
      'react-refresh': reactRefresh,
    },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': [
        'warn',
        { allowConstantExport: true },
      ],
    },
  },
  eslintConfigPrettier,
);
```

Key decisions:
- `tseslint.config()` is the flat-config wrapper (equivalent to `defineConfig` per D-05)
- `strictTypeChecked` + `stylisticTypeChecked` presets (per D-06)
- `projectService: true` (per D-07) -- uses TypeScript's built-in project service, NOT `project: ['./tsconfig.app.json']`
- `eslintConfigPrettier` at end disables rules that conflict with Prettier
- Ignores `e2e/` (Playwright tests use different patterns than application code)

**Step 2: Create Prettier config (per D-08)**

`.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 80,
  "tabWidth": 2
}
```

**Step 3: Initialize Husky + lint-staged (per D-03, D-08)**

Run:
```bash
npx husky init
```

This creates `.husky/` directory with a `pre-commit` file.

Overwrite `.husky/pre-commit` with:
```bash
npx lint-staged
```

Add `lint-staged` config to `package.json` (add this key at root level):
```json
"lint-staged": {
  "*.{ts,tsx}": ["eslint --fix", "prettier --write"],
  "*.{js,json,md,css}": ["prettier --write"]
}
```

**Step 4: Initialize shadcn/ui (per D-04)**

Run `npx shadcn@latest init` interactively or create files manually.

If running interactively, select:
- Style: New York
- Base color: Neutral
- CSS variables: Yes

If creating manually:

`components.json`:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/index.css",
    "baseColor": "neutral",
    "cssVariables": true
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

Note: `"rsc": false` because this is Vite, not Next.js. `"config": ""` because Tailwind v4 has no JS config file.

`src/lib/utils.ts`:
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

If shadcn init added CSS variables to `src/index.css`, keep them. The file should contain at minimum:
```css
@import "tailwindcss";
@import "tw-animate-css";
```

shadcn may add `@theme` blocks and `:root` / `.dark` CSS variable blocks -- this is expected for Tailwind v4.

**Step 5: Verify full toolchain**

Run:
```bash
npm run lint
npx prettier --check "src/**/*.{ts,tsx}"
npm run build
```

All three must pass with zero errors.
  </action>
  <verify>
    <automated>cd /Users/ragglesoft/Desktop/software-dev/robot-telemetry-dashboard && npm run lint && npx prettier --check "src/**/*.{ts,tsx}" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `eslint.config.js` exists (NOT `.mjs`) and contains `strictTypeChecked` and `stylisticTypeChecked`
    - `eslint.config.js` contains `projectService: true`
    - `eslint.config.js` contains `react-hooks` and `react-refresh` plugins
    - `eslint.config.js` contains `eslintConfigPrettier` (or `eslint-config-prettier`) as last config
    - `eslint.config.js` ignores array includes `'e2e/'`
    - `.prettierrc` exists and contains `"singleQuote": true`
    - `.husky/pre-commit` exists and contains `lint-staged`
    - `package.json` contains `"lint-staged"` key with `"*.{ts,tsx}"` entry
    - `components.json` exists and contains `"rsc": false`
    - `components.json` contains `"style": "new-york"`
    - `src/lib/utils.ts` exists and contains `export function cn`
    - No `tailwind.config.js`, `tailwind.config.ts`, or `postcss.config.js` exists
    - `npm run lint` exits 0 with no errors
    - `npm run build` exits 0 with no errors
  </acceptance_criteria>
  <done>ESLint 9 strict config, Prettier, Husky pre-commit hooks, lint-staged, and shadcn/ui are all configured and verified working. The full toolchain (lint + format + build) passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` produces zero errors, zero warnings
2. `npm run lint` passes with zero errors
3. `npx prettier --check "src/**/*.{ts,tsx}"` reports all formatted
4. No v3 artifacts remain (no `app/`, `components/`, `__tests__/`, `audit-*.png`)
5. No legacy config files exist (`tailwind.config.js`, `postcss.config.js`, `next.config.*`)
6. `node_modules/roslib` exists (installed successfully)
7. `vite.config.ts` does NOT contain `optimizeDeps` (roslib 2.x is ESM)
</verification>

<success_criteria>
- Vite 7 dev server starts and renders React 19 component
- Production build succeeds with zero errors/warnings
- ESLint 9 with strictTypeChecked catches type-level errors
- Prettier + Husky + lint-staged enforce formatting on commit
- shadcn/ui initialized with Tailwind v4 Vite plugin (no legacy config)
- All dependencies installed: react 19, react-is 19, roslib 2.x, recharts 3.x, d3 7.x, react-grid-layout 2.x, rxjs 7.x, zustand 5.x, react-router-dom
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding/01-01-SUMMARY.md`
</output>
