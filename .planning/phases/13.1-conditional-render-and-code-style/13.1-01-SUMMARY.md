---
phase: 13.1-conditional-render-and-code-style
plan: 01
status: complete
---

# 13.1-01 Summary: Promote cross-feature imports to shared layer

## Objective

Enforce one-way data flow (shared -> features -> app) by promoting all cross-feature imports to the shared layer.

## Tasks Completed

### Task 1: Promote hooks to src/hooks/ (a75784a)

**Moved:**

- `features/telemetry/shared/useRosConnection.ts` -> `src/hooks/useRosConnection.ts`
- `features/fleet/hooks/useFleetConnectionManager.ts` -> `src/hooks/useFleetConnectionManager.ts`
- `features/telemetry/lidar/hooks/useLidarCanvas.ts` -> `src/hooks/useLidarCanvas.ts`
- `features/telemetry/lidar/hooks/useLidarData.ts` -> `src/hooks/useLidarData.ts`

**Imports updated:** 25 files (13 source + 6 test mock paths + 6 import order fixes)

**Deleted:**

- `src/features/telemetry/lidar/hooks/` (empty directory removed)
- Original hook files from feature folders

### Task 2: Promote components + types to shared layer (0ccc986)

**Moved:**

- `features/telemetry/shared/NoConnectionOverlay.tsx` + `.types.ts` -> `src/components/shared/`
- `features/webrtc/components/VideoFeed.tsx` + `.test.tsx` -> `src/components/shared/`
- `features/control/components/ControlPad.tsx` + `.types.ts` + `.test.tsx` -> `src/components/shared/`

**Created:**

- `src/components/shared/VideoFeed.types.ts` (extracted from `webrtc.types.ts`)
- `src/types/panel.types.ts` (`PanelComponentProps` promoted)
- `src/types/control.types.ts` (`Direction`, `CONTROL_DEFAULT_TOPIC`, `CONTROL_MESSAGE_TYPE`)

**Deleted:**

- `src/features/webrtc/components/` (empty directory removed)
- Original component files from feature folders

**Modified (re-export stubs):**

- `features/panels/panel.types.ts` re-exports `PanelComponentProps` from `@/types/panel.types`
- `features/control/control.types.ts` re-exports all types from `@/types/control.types`
- `features/webrtc/webrtc.types.ts` trimmed to only `SignalingError` (feature-internal)

**Imports updated:** 25 files

## Cross-Feature Audit

Only `panel.registry.ts` has cross-feature imports (expected -- app-level composition):

- `@/features/control/components/ControlWidget`
- `@/features/slam/components/SlamMapWidget`
- `@/features/telemetry/*/components/*Widget` (5 widgets)

## Verification Results

| Check               | Result                                                                 |
| ------------------- | ---------------------------------------------------------------------- |
| `npm run lint`      | 2 pre-existing errors (panel.registry.ts unsafe-argument), 12 warnings |
| `npx tsc --noEmit`  | Pass (zero errors)                                                     |
| `npm test -- --run` | 510/510 pass                                                           |
| `npm run build`     | Success                                                                |
| Cross-feature audit | Only panel.registry.ts (expected)                                      |

## Deviations

- `ControlPad` in `src/components/shared/` imports `@/features/control/hooks/useControlPublisher`. This is a shared -> feature dependency, technically violating strict one-way data flow. Accepted per plan instructions.
- `useLidarCanvas` and `useLidarData` (now in `src/hooks/`) still import from `@/features/telemetry/lidar/lidar.types` and `lidar.utils`. These are type/utility imports that don't create circular dependencies.
- Pre-existing lint errors in `panel.registry.ts` (2 `@typescript-eslint/no-unsafe-argument`) and `RobotCard.tsx` (4 `@typescript-eslint/no-unsafe-*`) were not introduced by this refactor.
