---
phase: 13-component-conventions
plan: 02
type: execute
---

<objective>
Decompose remaining multi-component files: DataPlotWidget, ImuDigitalView, ImuWidget, SessionList, DashboardShell.

Purpose: Second wave targeting the remaining files with 2-3 components each. Smaller extractions but same strict conventions.
Output: Each file reduced to single component, sub-components extracted, all interfaces in .types.ts.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-component-conventions/13-CONTEXT.md
@.planning/phases/13-component-conventions/13-01-SUMMARY.md

# Files to refactor:

@src/features/telemetry/data-plot/components/DataPlotWidget.tsx
@src/features/telemetry/imu/components/ImuDigitalView.tsx
@src/features/telemetry/imu/components/ImuWidget.tsx
@src/features/recording/components/SessionList.tsx
@src/components/layout/DashboardShell.tsx

**Convention rules (strict):**

1. ONE component per .tsx file — no exceptions
2. ALL interfaces/types go to {ComponentName}.types.ts — even single-field, no exceptions
3. Sub-components as flat sibling files next to parent
4. Do NOT touch shadcn/ui files (src/components/ui/\*) — generated code
5. Pure refactoring — NO behavior changes
   </context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose DataPlotWidget.tsx and SessionList.tsx</name>
  <files>
    src/features/telemetry/data-plot/components/DataPlotWidget.tsx,
    src/features/telemetry/data-plot/components/DataPlotWidget.types.ts (new),
    src/features/telemetry/data-plot/components/TopicSelector.tsx (new),
    src/features/recording/components/SessionList.tsx,
    src/features/recording/components/SessionList.types.ts (new),
    src/features/recording/components/SessionRow.tsx (new)
  </files>
  <action>
**DataPlotWidget.tsx (2 components):**
1. Create `DataPlotWidget.types.ts` — move TopicSelectorProps
2. Create `TopicSelector.tsx` — move TopicSelector component
3. Update DataPlotWidget.tsx — import TopicSelector, keep main widget + drawChart helper (drawChart is a rendering utility, not a component)

**SessionList.tsx (2 components):**

1. Create `SessionList.types.ts` — move SessionListProps, SessionRowProps
2. Create `SessionRow.tsx` — move SessionRow component
3. Update SessionList.tsx — import SessionRow, keep main component

Pure file reorganization. Zero behavior changes.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>Both files have only their main component. Sub-components and types extracted. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose ImuDigitalView.tsx, ImuWidget.tsx, DashboardShell.tsx</name>
  <files>
    src/features/telemetry/imu/components/ImuDigitalView.tsx,
    src/features/telemetry/imu/components/ImuDigitalView.types.ts (new),
    src/features/telemetry/imu/components/MetricRow.tsx (new),
    src/features/telemetry/imu/components/ImuSection.tsx (new),
    src/features/telemetry/imu/components/ImuWidget.tsx,
    src/features/telemetry/imu/components/ImuWidget.types.ts (new),
    src/features/telemetry/imu/components/ViewToggle.tsx (new),
    src/components/layout/DashboardShell.tsx,
    src/components/layout/DashboardShell.types.ts (new),
    src/components/layout/DisconnectGuard.tsx (new)
  </files>
  <action>
**ImuDigitalView.tsx (3 components):**
1. Create `ImuDigitalView.types.ts` — move ImuDigitalViewProps, MetricRowProps, SectionProps
2. Create `MetricRow.tsx` — move MetricRow component
3. Create `ImuSection.tsx` — move Section component (rename to ImuSection to avoid naming collision with generic "Section")
4. Update ImuDigitalView.tsx — import sub-components

**ImuWidget.tsx (2 components):**

1. Create `ImuWidget.types.ts` — move ImuPanelConfig interface, ViewToggleProps
2. Create `ViewToggle.tsx` — move ViewToggle component
3. Update ImuWidget.tsx — import ViewToggle

**DashboardShell.tsx (2 components):**

1. Create `DashboardShell.types.ts` — if any interfaces exist, extract them
2. Create `DisconnectGuard.tsx` — move the DisconnectGuard component (invisible connection monitor)
3. Update DashboardShell.tsx — import DisconnectGuard

Pure file reorganization. Zero behavior changes.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>All 3 files have only their main component. Sub-components and types extracted. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes with 0 errors
- [ ] `npm run typecheck` passes with 0 errors
- [ ] `npm test -- --run` passes all 510+ tests
- [ ] `npm run build` succeeds
- [ ] No multi-component .tsx files remain (except shadcn/ui)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No behavior changes
- All multi-component violations resolved
  </success_criteria>

<output>
After completion, create `.planning/phases/13-component-conventions/13-02-SUMMARY.md`
</output>
