---
phase: 13-component-conventions
plan: 05
type: execute
---

<objective>
Restructure services into nested domain folders with co-located types.

Purpose: Services currently live as flat PascalCase files in src/services/ros/ and src/services/webrtc/. Each service needs its own subfolder with types extracted.
Output: Each service in its own folder with types. All service imports updated. Service barrel files deleted.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-component-conventions/13-CONTEXT.md
@.planning/knowledge/decisions/001-remove-barrel-files.md

# Current service files:

@src/services/ros/RosTransport.ts
@src/services/ros/RosServiceRegistry.ts
@src/services/ros/TopicSubscriber.ts
@src/services/ros/TopicPublisher.ts
@src/services/ros/TopicDiscovery.ts
@src/services/webrtc/WebRTCTransport.ts
@src/services/webrtc/WebRTCServiceRegistry.ts
@src/services/webrtc/SignalingClient.ts
@src/services/ros/index.ts
@src/services/webrtc/index.ts
@src/services/index.ts

**Target structure:**

```
src/services/ros/
  transport/
    RosTransport.ts
    RosTransport.types.ts
    RosTransport.test.ts
  registry/
    RosServiceRegistry.ts
    RosServiceRegistry.test.ts
  subscriber/
    TopicSubscriber.ts
    TopicSubscriber.types.ts
    TopicSubscriber.test.ts
  publisher/
    TopicPublisher.ts
    TopicPublisher.types.ts
    TopicPublisher.test.ts
  discovery/
    TopicDiscovery.ts
    TopicDiscovery.test.ts
```

**Rules:**

- Each service gets its own subfolder
- Extract interfaces to {ServiceName}.types.ts (only if the service has interfaces)
- Move test files into the same subfolder
- Delete barrel files: src/services/index.ts, src/services/ros/index.ts, src/services/webrtc/index.ts
- Update ALL imports across codebase
- Pure refactoring — NO behavior changes
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure ROS services</name>
  <files>
    src/services/ros/transport/ (new),
    src/services/ros/registry/ (new),
    src/services/ros/subscriber/ (new),
    src/services/ros/publisher/ (new),
    src/services/ros/discovery/ (new),
    src/services/ros/index.ts (delete),
    + all importing files
  </files>
  <action>
For each ROS service:

1. Create subfolder inside src/services/ros/
2. Move service file + test file into subfolder
3. Extract interfaces to .types.ts if the service has inline interfaces:
   - TopicSubscriber.ts has: TopicSubscriptionOptions
   - TopicPublisher.ts has: TopicPublisherHandle<T>
   - RosTransport, RosServiceRegistry, TopicDiscovery: check for inline interfaces, extract if found
4. Update all imports across codebase

Delete src/services/ros/index.ts barrel file.

Find all importing files:

```bash
grep -rn "from '@/services/ros" --include="*.ts" --include="*.tsx" src/ | grep -v services/ros
```

Update each to the new direct path.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run</verify>
<done>All 5 ROS services in subfolders with types. ros/index.ts deleted. All imports updated. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure WebRTC services + delete top barrel</name>
  <files>
    src/services/webrtc/transport/ (new),
    src/services/webrtc/registry/ (new),
    src/services/webrtc/signaling/ (new),
    src/services/webrtc/index.ts (delete),
    src/services/index.ts (delete),
    + all importing files
  </files>
  <action>
For each WebRTC service:

1. Create subfolder inside src/services/webrtc/
2. Move service file + test file into subfolder
3. Extract interfaces to .types.ts:
   - SignalingClient.ts has: SdpOffer, SdpAnswer
   - WebRTCTransport, WebRTCServiceRegistry: check for inline interfaces
4. Update all imports across codebase

Delete:

- src/services/webrtc/index.ts
- src/services/index.ts (top-level service barrel)

Verify no bare `@/services` or `@/services/ros` or `@/services/webrtc` barrel imports remain.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && grep -rn "from '@/services'" --include="_.ts" --include="_.tsx" src/ | grep -v "services/" | wc -l</verify>
<done>All 3 WebRTC services in subfolders with types. All service barrels deleted. Zero barrel imports. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 8 services in individual subfolders
- [ ] Types extracted where interfaces existed
- [ ] All 3 service barrel files deleted
- [ ] All imports updated to direct paths
- [ ] All tests pass, build succeeds
</verification>

<success_criteria>

- All services in subfolders
- All service barrels deleted
- All imports updated
- All tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-component-conventions/13-05-SUMMARY.md`
</output>
