---
phase: 13-component-conventions
plan: 06
type: execute
---

<objective>
Extract hook types, extract component constants, and delete ALL remaining barrel files.

Purpose: Final sweep — hook interfaces to .types.ts, component-specific constants to .constants.ts, and every remaining index.ts barrel file deleted with imports updated.
Output: Zero inline interfaces in hooks, zero inline constants in components, zero barrel files in application code. Phase 13 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-component-conventions/13-CONTEXT.md
@.planning/knowledge/decisions/001-remove-barrel-files.md
@.planning/phases/13-component-conventions/13-04-SUMMARY.md
@.planning/phases/13-component-conventions/13-05-SUMMARY.md

**Hooks with inline interfaces (8 total):**

- useControlPublisher.ts → UseControlPublisherResult
- usePilotMode.ts → UsePilotModeResult
- useRecording.ts → UseRecordingReturn
- usePlayback.ts → UsePlaybackReturn
- useSlamCanvas.ts → UseSlamCanvasOptions, UseSlamCanvasResult
- useSlamData.ts → UseSlamDataResult
- useElementSize.ts → ElementSize

**Components with inline constants (~12 instances):**

- PilotLidarMinimap.tsx → MINIMAP_SIZE, MINIMAP_SCALE
- PanelGrid.tsx → BREAKPOINTS, COLS, ROW_HEIGHT
- DataPlotWidget.tsx → MARGIN, LINE_COLOURS
- ImuPlotView.tsx → MARGIN, LINE_COLORS
- VelocitySliders.tsx → MIN_VEL, MAX_VEL, STEP_VEL

**Remaining barrel files to delete (after plans 04+05 remove store/service barrels):**

- src/components/index.ts
- src/components/shared/index.ts
- src/components/ui/index.ts (KEEP — shadcn generated)
- src/features/connections/index.ts
- src/features/control/index.ts
- src/features/fleet/index.ts
- src/features/panels/index.ts
- src/features/pilot-mode/index.ts
- src/features/recording/index.ts
- src/features/slam/index.ts
- src/features/telemetry/index.ts + all sub-barrels
- src/features/webrtc/index.ts
- src/types/index.ts

**Rules:**

- ALL hook interfaces → {hookName}.types.ts
- Component-specific constants → {ComponentName}.constants.ts
- Delete ALL index.ts barrels EXCEPT src/components/ui/index.ts (shadcn generated)
- Update EVERY import that referenced a barrel to use direct path
- Pure refactoring — NO behavior changes
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Extract hook types to .types.ts files</name>
  <files>
    src/features/control/hooks/useControlPublisher.types.ts (new),
    src/features/pilot-mode/hooks/usePilotMode.types.ts (new),
    src/features/recording/hooks/useRecording.types.ts (new),
    src/features/recording/hooks/usePlayback.types.ts (new),
    src/features/slam/hooks/useSlamCanvas.types.ts (new),
    src/features/slam/hooks/useSlamData.types.ts (new),
    src/hooks/useElementSize.types.ts (new),
    + the 7 corresponding hook .ts files
  </files>
  <action>
For each hook with inline interfaces:

1. Create `{hookName}.types.ts` next to the hook
2. Move the interface declaration (add `export` if not already)
3. Update the hook file with `import type { XxxResult } from './{hookName}.types'`

Process all 7 hooks. Mechanical extraction — same pattern as component types.

After extraction, verify:

```bash
grep -rn "^interface \|^export interface " --include="*.ts" src/features/*/hooks/ src/hooks/ | grep -v test
```

Should return ZERO results.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run</verify>
<done>Zero inline interfaces in hook files. All 7 types files created. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract component constants to .constants.ts files</name>
  <files>
    src/features/pilot-mode/components/PilotLidarMinimap.constants.ts (new),
    src/features/panels/components/PanelGrid.constants.ts (new),
    src/features/telemetry/data-plot/components/DataPlotWidget.constants.ts (new),
    src/features/telemetry/imu/components/ImuPlotView.constants.ts (new),
    src/features/control/components/VelocitySliders.constants.ts (new),
    + the 5 corresponding .tsx files
  </files>
  <action>
For each component with inline UPPER_SNAKE_CASE constants:

1. Create `{ComponentName}.constants.ts` next to the component
2. Move ALL constant declarations (const UPPER_SNAKE = ...) to the constants file
3. Export each constant
4. Update the .tsx file with import from constants file

Process all 5 component files. Constants that are genuinely trivial (used once, 1 line) can stay inline — use judgment, but default to extracting.

Note: `as const` assertions should be preserved when moving.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run</verify>
<done>Component-specific constants extracted to .constants.ts files. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Delete ALL remaining barrel files + update imports</name>
  <files>
    All index.ts files listed in context (delete ~20 files),
    + every file in src/ that imports from a barrel path
  </files>
  <action>
This is the biggest task — deleting barrels and fixing every import.

**Step 1: Catalog what each barrel re-exports**

For each barrel file, read it to understand what it exports and where the actual source files are. This mapping tells you how to rewrite imports.

Example: if `src/features/panels/index.ts` re-exports `PanelGrid` from `./components/PanelGrid`, then every import of `PanelGrid` from `@/features/panels` must become `@/features/panels/components/PanelGrid`.

**Step 2: For each barrel, find all consumers**

```bash
# Example for panels
grep -rn "from '@/features/panels'" --include="*.ts" --include="*.tsx" src/ | grep -v "features/panels/"
```

**Step 3: Update each consumer import to use direct path**

Replace barrel path with direct source path. Handle multi-import statements (barrel may re-export multiple things from different files — split into separate imports).

**Step 4: Delete the barrel file**

After all consumers updated, delete the index.ts.

**Process ALL barrels EXCEPT src/components/ui/index.ts (shadcn generated)**

Barrels to delete:

- src/components/index.ts
- src/components/shared/index.ts
- src/features/connections/index.ts
- src/features/control/index.ts
- src/features/fleet/index.ts
- src/features/panels/index.ts
- src/features/pilot-mode/index.ts
- src/features/recording/index.ts
- src/features/slam/index.ts
- src/features/telemetry/index.ts
- src/features/telemetry/data-plot/index.ts
- src/features/telemetry/depth-camera/index.ts
- src/features/telemetry/imu/index.ts
- src/features/telemetry/lidar/index.ts
- src/features/telemetry/shared/index.ts
- src/features/telemetry/topic-list/index.ts
- src/features/webrtc/index.ts
- src/types/index.ts

**Step 5: Final verification scan**

```bash
# Should only find src/components/ui/index.ts
find src -name "index.ts" -not -path "*/node_modules/*" | grep -v "components/ui"
```

Must return ZERO results (except possibly src/main.tsx entry point and router/index.tsx which are entry points, not barrels).

This completes Phase 13.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npm run build && echo "=== Remaining barrels ===" && find src -name "index.ts" -not -path "_/node_modules/_" | grep -v "components/ui"</verify>
<done>ALL barrel files deleted (except shadcn/ui). ALL imports use direct paths. Zero barrel imports remain. All 510+ tests pass. Build succeeds. Phase 13 complete.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm test -- --run` passes all 510+ tests
- [ ] `npm run build` succeeds
- [ ] Zero inline interfaces in hooks
- [ ] Component constants in .constants.ts files
- [ ] Zero barrel files remain (except shadcn/ui)
- [ ] Zero barrel imports remain
- [ ] Phase 13 COMPLETE
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Zero inline interfaces anywhere (components, hooks, stores, services)
- Zero inline constants in components
- Zero barrel files in application code
- Every import is a direct path to source
- Phase 13: Component Conventions COMPLETE
  </success_criteria>

<output>
After completion, create `.planning/phases/13-component-conventions/13-06-SUMMARY.md`
</output>
