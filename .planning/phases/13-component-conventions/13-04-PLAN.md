---
phase: 13-component-conventions
plan: 04
type: execute
---

<objective>
Restructure all 6 Zustand stores into domain folders with co-located types.

Purpose: Stores are currently flat files in src/stores/. Each store has 3 files (store, test, and now needs types). Move into domain folders per ADR-002.
Output: 6 domain folders, each with {domain}.store.ts + {domain}.types.ts + {domain}.test.ts. All store imports updated across codebase.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-component-conventions/13-CONTEXT.md
@.planning/knowledge/decisions/002-store-domain-folders.md

# Current store files:

@src/stores/connections.store.ts
@src/stores/control.store.ts
@src/stores/layout.store.ts
@src/stores/ros.store.ts
@src/stores/ui.store.ts
@src/stores/webrtc.store.ts
@src/stores/index.ts

**Target structure per store:**

```
src/stores/{domain}/
  {domain}.store.ts    # the store
  {domain}.types.ts    # extracted interfaces
  {domain}.test.ts     # tests (renamed from .store.test.ts)
```

**Rules:**

- Extract ALL interfaces from each store to {domain}.types.ts
- Move store + test into domain folder
- Rename test: `connections.store.test.ts` → `connections.test.ts`
- Update ALL imports across codebase: `@/stores/connections.store` → `@/stores/connections/connections.store`
- Delete src/stores/index.ts barrel file (ADR-001)
- Pure refactoring — NO behavior changes
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure connections, control, and layout stores</name>
  <files>
    src/stores/connections/ (new folder),
    src/stores/control/ (new folder),
    src/stores/layout/ (new folder),
    + all files that import from these stores
  </files>
  <action>
For each of the 3 stores (connections, control, layout):

1. Create domain folder: `src/stores/{domain}/`
2. Create `{domain}.types.ts` — extract ALL interfaces from the store file
3. Move `{domain}.store.ts` into the folder, update its imports to reference the new types file
4. Move `{domain}.store.test.ts` into the folder as `{domain}.test.ts`
5. Find ALL files that import from `@/stores/{domain}.store` and update to `@/stores/{domain}/{domain}.store`

Use grep to find all importing files:

```bash
grep -rn "from '@/stores/connections.store'" --include="*.ts" --include="*.tsx" src/
```

Repeat for control and layout. Update every import path.

connections.store has: ConnectionsState, ConnectionsActions (2 interfaces)
control.store has: RobotControlEntry, ControlState, ControlActions (3 interfaces)
layout.store has: LayoutStoreState, LayoutStoreActions (2 interfaces)
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run</verify>
<done>3 stores in domain folders with types extracted. All imports updated. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure ros, webrtc, and ui stores + delete barrel</name>
  <files>
    src/stores/ros/ (new folder),
    src/stores/webrtc/ (new folder),
    src/stores/ui/ (new folder),
    src/stores/index.ts (delete),
    + all files that import from these stores or the barrel
  </files>
  <action>
Same process for ros, webrtc, ui stores:

1. Create domain folders, extract types, move files, rename tests
2. Update all imports across codebase

ros.store has: RosConnectionEntry, RosState, RosActions (3 interfaces)
webrtc.store has: WebRTCConnectionEntry, WebRTCState, WebRTCActions (3 interfaces)
ui.store has: UIState, UIActions (2 interfaces)

3. Delete `src/stores/index.ts` barrel file
4. Find any imports from `@/stores` (the barrel) and update to direct store paths
5. Also check for `from '@/stores'` imports — these need to be split into individual direct imports

After deletion, verify no file imports from `@/stores/index` or bare `@/stores`.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && grep -rn "from '@/stores'" --include="_.ts" --include="_.tsx" src/ | grep -v "stores/" | head -5; echo "Should show 0 bare barrel imports"</verify>
<done>All 6 stores in domain folders. src/stores/index.ts deleted. Zero barrel imports. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` passes
- [ ] `npx tsc --noEmit` passes
- [ ] `npm test -- --run` passes all 510+ tests
- [ ] `npm run build` succeeds
- [ ] 6 domain folders exist in src/stores/
- [ ] Each folder has: {domain}.store.ts, {domain}.types.ts, {domain}.test.ts
- [ ] src/stores/index.ts deleted
- [ ] Zero imports from bare `@/stores` path
</verification>

<success_criteria>

- All stores in domain folders with types
- Stores barrel deleted
- All imports updated
- All tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/13-component-conventions/13-04-SUMMARY.md`
</output>
