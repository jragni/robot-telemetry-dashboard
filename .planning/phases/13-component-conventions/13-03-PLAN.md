---
phase: 13-component-conventions
plan: 03
type: execute
---

<objective>
Extract ALL remaining inline interfaces from single-component .tsx files to .types.ts files.

Purpose: After plans 01 and 02 fixed multi-component files, this plan sweeps every remaining .tsx file to extract inline interfaces. Zero inline interfaces should remain in any .tsx component file.
Output: Every component's types in a co-located .types.ts file. Maximum consistency across the codebase.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-component-conventions/13-CONTEXT.md
@.planning/phases/13-component-conventions/13-01-SUMMARY.md
@.planning/phases/13-component-conventions/13-02-SUMMARY.md

**Files with inline interfaces (after plans 01+02 fix multi-component issues):**

pilot-mode (4 files):

- PilotLayout.tsx — PilotLayoutProps
- PilotMobileLayout.tsx — PilotMobileLayoutProps
- PilotExitButton.tsx — PilotExitButtonProps
- PilotLidarMinimap.tsx — PilotLidarMinimapProps

panels (4 files):

- PanelToolbar.tsx — PanelToolbarProps
- PanelPlaceholder.tsx — PanelPlaceholderProps
- PanelFrame.tsx — PanelFrameProps
- PanelGrid.tsx — PanelGridProps
- AddPanelDialog.tsx — AddPanelDialogProps

recording (1 file):

- RecordingControls.tsx — RecordingControlsProps

telemetry (3 files):

- TopicRow.tsx — TopicRowProps
- NoConnectionOverlay.tsx — NoConnectionOverlayProps
- ImuPlotView.tsx — ImuPlotViewProps

fleet (4 files):

- SplitPilotGrid.tsx — SplitPilotGridProps
- UnifiedCommandPanel.tsx — UnifiedCommandPanelProps
- RobotCard.tsx — RobotCardProps
- MiniPilotView.tsx — MiniPilotViewProps
- FleetGrid.tsx — FleetGridProps

control (3 files):

- VelocitySliders.tsx — VelocitySlidersProps
- TopicSelector.tsx — TopicSelectorProps
- ControlPad.tsx — ControlPadProps, DirButtonConfig

shared (3 files):

- StatusIndicator.tsx — StatusIndicatorProps
- LoadingSpinner.tsx — LoadingSpinnerProps
- DataCard.tsx — DataCardProps

router (1 file):

- RequiresConnection.tsx — RequiresConnectionProps

**Convention rules:** ALL interfaces extracted to .types.ts — no exceptions, no size threshold.
**Pure refactoring — NO behavior changes.**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract types from features/pilot-mode, features/panels, features/recording</name>
  <files>
    src/features/pilot-mode/components/PilotLayout.types.ts (new),
    src/features/pilot-mode/components/PilotMobileLayout.types.ts (new),
    src/features/pilot-mode/components/PilotExitButton.types.ts (new),
    src/features/pilot-mode/components/PilotLidarMinimap.types.ts (new),
    src/features/panels/components/PanelToolbar.types.ts (new),
    src/features/panels/components/PanelPlaceholder.types.ts (new),
    src/features/panels/components/PanelFrame.types.ts (new),
    src/features/panels/components/PanelGrid.types.ts (new),
    src/features/panels/components/AddPanelDialog.types.ts (new),
    src/features/recording/components/RecordingControls.types.ts (new)
  </files>
  <action>
For each file listed:
1. Create `{ComponentName}.types.ts` next to the component
2. Move the interface declaration to the types file
3. Add `export` to the interface if not already exported
4. Update the .tsx file to `import type { XxxProps } from './{ComponentName}.types'`
5. Ensure the import follows the project's import ordering convention

Process all 10 files. Each extraction is mechanical — move interface, add import.

Do NOT change any component behavior. Do NOT modify the interface content. Pure relocation.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>Zero inline interfaces remain in pilot-mode, panels, and recording .tsx component files. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract types from features/telemetry, features/fleet, features/control</name>
  <files>
    src/features/telemetry/topic-list/components/TopicRow.types.ts (new),
    src/features/telemetry/shared/NoConnectionOverlay.types.ts (new),
    src/features/telemetry/imu/components/ImuPlotView.types.ts (new),
    src/features/fleet/components/SplitPilotGrid.types.ts (new),
    src/features/fleet/components/UnifiedCommandPanel.types.ts (new),
    src/features/fleet/components/RobotCard.types.ts (new),
    src/features/fleet/components/MiniPilotView.types.ts (new),
    src/features/fleet/components/FleetGrid.types.ts (new),
    src/features/control/components/VelocitySliders.types.ts (new),
    src/features/control/components/TopicSelector.types.ts (new),
    src/features/control/components/ControlPad.types.ts (new)
  </files>
  <action>
Same mechanical process for each file:
1. Create `{ComponentName}.types.ts`
2. Move interface(s) to types file
3. Export the interface
4. Update .tsx with `import type` from types file

Note: ControlPad.tsx has TWO interfaces (ControlPadProps + DirButtonConfig) — extract both.

Process all 11 files. Pure relocation.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>Zero inline interfaces remain in telemetry, fleet, and control .tsx component files. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Extract types from components/shared, components/layout, router</name>
  <files>
    src/components/shared/StatusIndicator.types.ts (new),
    src/components/shared/LoadingSpinner.types.ts (new),
    src/components/shared/DataCard.types.ts (new),
    src/router/guards/RequiresConnection.types.ts (new)
  </files>
  <action>
Same mechanical process for the remaining 4 files:
1. Create types file
2. Move interface
3. Export + update import in .tsx

Note: LoadingSpinner uses `VariantProps<typeof spinnerVariants>` in its interface — the types file will need to import that from the component or from CVA. The simplest approach: keep the `spinnerVariants` definition in LoadingSpinner.tsx and export it, then import it in the types file. Or move the variants to the types file too. Choose whichever keeps both files clean.

After all extractions, do a final verification scan:

```bash
grep -rn "^interface \|^export interface " --include="*.tsx" src/ | grep -v test | grep -v "components/ui"
```

This should return ZERO results (excluding shadcn/ui files and test files).

Phase 13 complete after this task.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run && echo "=== Remaining inline interfaces ===" && grep -rn "^interface \|^export interface " --include="\*.tsx" src/ | grep -v test | grep -v "components/ui" | wc -l</verify>
<done>Zero inline interfaces in any .tsx component file (except shadcn/ui). Final scan returns 0. All tests pass. npm run build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint` passes with 0 errors
- [ ] `npm run typecheck` passes with 0 errors
- [ ] `npm test -- --run` passes all 510+ tests
- [ ] `npm run build` succeeds
- [ ] `grep -rn "^interface \|^export interface " --include="*.tsx" src/ | grep -v test | grep -v "components/ui"` returns 0 results
- [ ] No multi-component .tsx files remain (except shadcn/ui)
- [ ] Phase 13 complete
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Zero inline interfaces in any .tsx component file (excluding shadcn/ui)
- Zero multi-component .tsx files (excluding shadcn/ui)
- All 510+ tests pass
- Phase 13: Component Conventions complete
  </success_criteria>

<output>
After completion, create `.planning/phases/13-component-conventions/13-03-SUMMARY.md`
</output>
