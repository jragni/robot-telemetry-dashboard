---
phase: 14-view-wiring
plan: 02
type: execute
---

<objective>
Dark theme resize handle styling + always-on drag/resize (remove edit mode).

Purpose: Resize handles are invisible on dark theme and panels require an edit mode toggle. Fix handle visibility with electric blue accents and make panels always draggable/resizable via header + corner grips.
Output: Visible resize handles on all 4 corners, full-header drag, no edit mode dependency.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-view-wiring/14-CONTEXT.md
@.planning/phases/14-view-wiring/14-RESEARCH.md
@.planning/phases/14-view-wiring/14-01-SUMMARY.md

# Key source files:

@src/style.css
@src/features/panels/components/PanelGrid.tsx
@src/features/panels/components/PanelFrame.tsx
@src/components/shared/DataCard.tsx

**From RESEARCH.md:**

- `resizeHandles={['nw', 'ne', 'sw', 'se']}` enables all 4 corners
- `isDraggable={true}` + `isResizable={true}` permanently (no editMode)
- `draggableHandle=".panel-drag-handle"` — expand selector to full header
- `draggableCancel=".panel-action-button"` — exclude buttons from drag
- CSS overrides on `.react-resizable-handle` for dark theme

**From CONTEXT.md:**

- Always editable, no mode toggle
- Nothing moves by accident — header is drag zone, corners are resize
- Visible corner grips on all 4 corners, electric blue
- × button shows on hover only

**Rules:**

- Zero lint warnings
- Use `<Show>` for conditional rendering
- Types in .types.ts
- Full quality gate: `npm run lint && npx tsc --noEmit && npm test -- --run && npm run build`
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Dark theme CSS overrides for resize handles + drag placeholder</name>
  <files>src/style.css</files>
  <action>
Add CSS overrides AFTER the existing styles in src/style.css:

1. Make resize handles always partially visible (not just on hover):
   - `.react-grid-item > .react-resizable-handle` → `opacity: 0.4`
   - `.react-grid-item:hover > .react-resizable-handle` → `opacity: 1`

2. Electric blue handle accents (replace invisible black):
   - `.react-grid-item > .react-resizable-handle::after` → border colors `oklch(0.62 0.22 230 / 0.6)`
   - Hover state → full opacity `oklch(0.62 0.22 230)`

3. Remove default black SVG background:
   - `.react-resizable-handle` → `background-image: none !important`

4. Electric blue drag placeholder (replace default red):
   - `.react-grid-item.react-grid-placeholder` → `background: oklch(0.62 0.22 230)`, `opacity: 0.15`, `border-radius: 2px`

5. Ensure handles have sufficient z-index:
   - `.react-grid-item > .react-resizable-handle` → `z-index: 10`

See RESEARCH.md code examples for exact CSS.
</action>
<verify>npm run lint && npx tsc --noEmit && npm run build</verify>
<done>Resize handles visible with electric blue on dark theme. Drag placeholder is blue. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Always-on drag/resize — remove editMode, add 4-corner handles, full-header drag</name>
  <files>
    src/features/panels/components/PanelGrid.tsx,
    src/features/panels/components/PanelFrame.tsx,
    src/components/shared/DataCard.tsx,
    src/stores/layout/layout.store.ts
  </files>
  <action>
**PanelGrid.tsx changes:**
1. Remove `editMode` from store subscription — no longer needed
2. Remove `canInteract` variable — always true now
3. Set `isDraggable={true}` permanently (was `isDraggable={canInteract}`)
4. Set `isResizable={true}` permanently (was `isResizable={canInteract}`)
5. Add `resizeHandles={['nw', 'ne', 'sw', 'se']}` prop to Responsive
6. Add `draggableCancel=".panel-action-button"` prop to Responsive
7. Keep `draggableHandle=".panel-drag-handle"` — but we'll expand what matches this selector
8. Remove mobile breakpoint check for canInteract — panels always interactive

**PanelFrame.tsx changes:**

1. Remove `editMode` prop — no longer conditional
2. Drag handle: Remove the small GripVertical icon. Instead, make the entire header bar the drag handle
3. × button: Change from always-visible-in-edit-mode to show-on-hover-only. Add `opacity-0 group-hover:opacity-100 transition-opacity` classes + `.panel-action-button` class
4. The `headerActions` should always render the × button (not conditionally on editMode)

**DataCard.tsx changes:**

1. Add `.panel-drag-handle` class to the header div (the one with `flex items-center justify-between`)
2. Add `cursor-grab active:cursor-grabbing` to the header div
3. Add `group` class to the outer card div (for group-hover on × button)
4. Ensure `.panel-action-button` class is on the × button (for draggableCancel)

**layout.store.ts:**

1. Check if `editMode` and `setEditMode` are still needed by anything else. If only used by PanelGrid/PanelFrame (which we're removing), remove them from the store.
2. If other components reference editMode, leave it but it's no longer used by the panel system.

Run grep to check: `grep -rn "editMode\|setEditMode" --include="*.ts" --include="*.tsx" src/ | grep -v test`
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npm run build</verify>
<done>Panels always draggable (via full header) and resizable (via 4 corners). No editMode dependency. × button shows on hover. draggableCancel prevents button clicks from triggering drag. All tests pass.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` — ZERO warnings
- [ ] `npx tsc --noEmit` — ZERO errors
- [ ] `npm test -- --run` — ALL tests pass
- [ ] `npm run build` — succeeds
- [ ] Resize handles visible on all 4 corners with electric blue
- [ ] Full header bar is draggable with grab cursor
- [ ] × button appears on hover, doesn't trigger drag
- [ ] No editMode toggle anywhere in panel system
</verification>

<success_criteria>

- Resize handles visible, themed, on all 4 corners
- Full header drag with correct cursors
- × on hover only
- No editMode in panel system
- All tests pass, zero lint warnings
  </success_criteria>

<output>
After completion, create `.planning/phases/14-view-wiring/14-02-SUMMARY.md`
</output>
