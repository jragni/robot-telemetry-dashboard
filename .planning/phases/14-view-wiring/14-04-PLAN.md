---
phase: 14-view-wiring
plan: 04
type: execute
---

<objective>
Move hamburger menu button below Header + final cleanup.

Purpose: The sidebar toggle (hamburger) should be positioned below the Header bar for better visual hierarchy (ISS-006). Also clean up any remaining editMode references and update knowledge base/specs.
Output: Hamburger below header, clean codebase, Phase 14 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-view-wiring/14-CONTEXT.md
@.planning/phases/14-view-wiring/14-03-SUMMARY.md
@.planning/ISSUES.md

# Key source files:

@src/components/layout/Header.tsx
@src/components/layout/DashboardShell.tsx
@src/views/DashboardView.tsx
@src/views/MapView.tsx

**ISS-006:** Move hamburger menu button below Header — better visual hierarchy for telemetry dashboard.

**Rules:**

- Zero lint warnings
- Full quality gate
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Move sidebar toggle below Header + cleanup</name>
  <files>
    src/components/layout/Header.tsx,
    src/components/layout/DashboardShell.tsx,
    src/views/DashboardView.tsx,
    src/views/MapView.tsx
  </files>
  <action>
**Move hamburger button:**

Currently the hamburger (sidebar toggle) is inside the Header component. Move it to DashboardShell, positioned between the Header and the main content area. Options:

1. Extract the hamburger button from Header.tsx
2. Place it in DashboardShell.tsx, in a thin bar between `<Header />` and the `<div className="flex flex-1 overflow-hidden">` containing sidebar + main content
3. Style: small, left-aligned, subtle — `px-2 py-1 border-b border-border` strip
4. Or: place it at the top of the sidebar area itself (always visible even when sidebar collapsed)

Choose whichever creates the cleanest visual hierarchy. The hamburger should feel like it controls the sidebar, not be part of the navigation header.

**Cleanup:**

1. Run `grep -rn "editMode\|setEditMode" --include="*.ts" --include="*.tsx" src/ | grep -v test` to find any remaining editMode references
2. If layout.store.ts still has editMode/setEditMode that nothing uses, remove them
3. Update the store types file if editMode types are removed
4. Update any tests that reference editMode

**Update ISSUES.md:**
Close ISS-004, ISS-005, ISS-006.

Verify: `npm run lint && npx tsc --noEmit && npm test -- --run && npm run build`
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npm run build && grep -rn "editMode" --include="_.ts" --include="_.tsx" src/ | grep -v test | grep -v ".types." | wc -l</verify>
<done>Hamburger below header. Zero editMode references in non-test code (or only in store if kept for future use). ISS-004/005/006 closed. All tests pass. Phase 14 complete.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint` — ZERO warnings
- [ ] `npx tsc --noEmit` — ZERO errors
- [ ] `npm test -- --run` — ALL tests pass
- [ ] `npm run build` — succeeds
- [ ] Hamburger positioned below header
- [ ] ISS-004, ISS-005, ISS-006 closed in ISSUES.md
- [ ] Phase 14: View Wiring COMPLETE
</verification>

<success_criteria>

- Hamburger in improved position
- All open Phase 14 issues closed
- Full quality gate passes
- Phase 14 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/14-view-wiring/14-04-SUMMARY.md`
</output>
