---
phase: 14-view-wiring
plan: 01
type: execute
---

<objective>
Wire existing feature components into placeholder views and fix integration gaps from the overnight build.

Purpose: DashboardView and MapView are still stubs showing placeholder text despite all feature components being fully built and tested. Recording UI has no entry point. This phase connects the dots.
Output: Functional Dashboard with panel grid, functional Map with SLAM renderer, recording accessible via panel, minor cleanup.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONCERNS.md

# Key source files:

@src/views/DashboardView.tsx
@src/views/MapView.tsx
@src/features/panels/components/PanelGrid.tsx
@src/features/panels/panel.registry.ts
@src/features/panels/panel.defaults.ts
@src/features/panels/panel.types.ts
@src/features/slam/index.ts
@src/features/recording/index.ts
@src/features/recording/components/RecordingControls.tsx
@src/stores/connections.store.ts
@src/stores/layout.store.ts
@src/router/index.tsx

**Tech stack available:** React 19, Zustand 5, RxJS 7, react-grid-layout 2, Tailwind 4, shadcn/ui
**Established patterns:**

- PanelGrid takes `viewId` and `robotId` props, reads layout from layout.store
- Widgets receive `PanelComponentProps` ({ robotId, panelId })
- Panel types registered in `panel.registry.ts`, defaults in `panel.defaults.ts`
- Views are route-level components rendered inside DashboardShell via Outlet
- `useConnectionsStore((s) => s.activeRobotId)` provides active robot context

**Constraining decisions:**

- Panel system uses react-grid-layout with responsive breakpoints (lg/md/sm)
- Default layouts already defined for dashboard, fleet, map, pilot views
- PanelTypeId union type must be extended to add new panel types
- Recording feature exports RecordingControls, SessionList, PlaybackControls components
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Wire DashboardView to PanelGrid</name>
  <files>src/views/DashboardView.tsx</files>
  <action>
Replace the placeholder content in DashboardView with the PanelGrid component.

- Import `PanelGrid` from `@/features/panels`
- Import `useConnectionsStore` from `@/stores/connections.store`
- Read `activeRobotId` from the connections store
- Render `<PanelGrid viewId="dashboard" robotId={activeRobotId ?? undefined} />` as the main content
- Wrap in a `div` with `className="flex flex-1 overflow-hidden"` to fill the DashboardShell main area
- Do NOT add any conditional rendering for "no robot connected" — PanelGrid's individual widgets handle that via NoConnectionOverlay

The default dashboard layout is already defined in `panel.defaults.ts` with: video-feed, lidar-view, imu-display, connection-status, topic-list panels.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>DashboardView renders PanelGrid with viewId="dashboard", passes activeRobotId, no placeholder text remains, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire MapView to PanelGrid</name>
  <files>src/views/MapView.tsx</files>
  <action>
Replace the placeholder content in MapView with the PanelGrid component.

- Import `PanelGrid` from `@/features/panels`
- Import `useConnectionsStore` from `@/stores/connections.store`
- Read `activeRobotId` from the connections store
- Render `<PanelGrid viewId="map" robotId={activeRobotId ?? undefined} />` as the main content
- Wrap in a `div` with `className="flex flex-1 overflow-hidden"` to fill the DashboardShell main area

The default map layout is already defined in `panel.defaults.ts` with: map-view (large SLAM widget), connection-status, topic-list panels.
</action>
<verify>npm run lint && npm run typecheck && npm test -- --run</verify>
<done>MapView renders PanelGrid with viewId="map", passes activeRobotId, no placeholder text remains, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dashboard and Map views now render real panel grids with draggable widgets instead of placeholder text. Dashboard shows video, LiDAR, IMU, connection status, and topic list panels. Map shows SLAM occupancy grid, connection status, and topic list panels.</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Visit: http://localhost:5173/robot-telemetry-dashboard/dashboard
    3. Verify: Panel grid renders with 5 widget panels (may show "No Connection" overlays without a robot — that's expected)
    4. Verify: Panels are draggable and resizable (click the edit/lock toggle if present)
    5. Visit: http://localhost:5173/robot-telemetry-dashboard/map
    6. Verify: Map view renders with SLAM map panel (large) and two smaller panels
    7. Resize browser window to verify responsive breakpoints work (panels reflow)
    8. Check: No console errors, no blank pages, no placeholder text
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint` passes with 0 errors
- [ ] `npm run typecheck` passes with 0 errors
- [ ] `npm test -- --run` passes all 510+ tests
- [ ] `npm run build` succeeds
- [ ] DashboardView renders PanelGrid (no placeholder text)
- [ ] MapView renders PanelGrid (no placeholder text)
- [ ] Both views pass activeRobotId from connections store
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Dashboard shows draggable panel grid with telemetry widgets
- Map shows SLAM map panel with supporting widgets
- Responsive layouts work at all breakpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/13-view-wiring/13-01-SUMMARY.md`
</output>
