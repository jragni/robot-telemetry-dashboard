---
phase: 15-mobile-responsive-nav
plan: 05
type: execute
---

<objective>
Final quality gate: full E2E suite, Playwright visual testing, cleanup, Phase 15 complete.

Purpose: Run comprehensive E2E tests across all viewports, verify all mobile behaviors work end-to-end, fix any remaining issues. Close Phase 15.
Output: All E2E tests pass. All responsive behaviors verified. Phase 15 complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/15-mobile-responsive-nav/15-CONTEXT.md
@.planning/phases/15-mobile-responsive-nav/15-04-SUMMARY.md
@.planning/ISSUES.md

**Full test suite to run:**

- `npm run lint` — ZERO warnings
- `npx tsc --noEmit` — ZERO errors
- `npm test -- --run` — ALL unit tests pass
- `npx playwright test` — ALL E2E tests pass (smoke + responsive-panels + mobile-responsive)

**Remaining cleanup:**

- Update ISSUES.md: close ISS-007 status if video fix is separate phase
- Verify no remaining ISS-008 regression (responsive panel grid)
- Check for any unused imports or dead code from Phase 15 changes
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate + fix any remaining issues</name>
  <files>
    Any files needing fixes
  </files>
  <action>
Run the complete quality gate:
```bash
npm run lint && npx tsc --noEmit && npm test -- --run && npm run build && npx playwright test --reporter=list
```

If ANY failures:

1. Diagnose the root cause
2. Fix with legitimate code changes (no suppression)
3. Re-run the gate
4. Loop until all pass

Pay special attention to:

- The responsive-panels E2E tests (ISS-008 regression)
- The mobile-responsive E2E tests (new Phase 15 tests)
- The smoke E2E tests (basic navigation)
- Any lint warnings introduced by new components

Clean up any unused imports, dead code, or stale references from Phase 15 changes.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npm run build && npx playwright test --reporter=list</verify>
<done>Full quality gate passes: zero lint, zero TS errors, all unit tests, build, all E2E tests. Phase 15 complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete mobile responsive experience: bottom tab bar, swipeable panel carousel, kebab menus, pilot tab with robot picker, responsive transitions between desktop and mobile. All built TDD — tests written first.</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open Chrome DevTools → iPhone 14 (390×844)
    3. Dashboard: bottom tabs visible, swipeable panels, dot indicators
    4. Swipe between panels — dots update
    5. Tap ⋮ on a panel → dropdown menu with actions
    6. Tap Fleet tab → Fleet view loads
    7. Tap Map tab → Map view with swipeable panels
    8. Tap Pilot tab → Robot picker (or empty state)
    9. Switch to Desktop (1440×900)
    10. Verify: bottom bar gone, header nav back, grid panels
    11. Right-click panel → context menu (desktop behavior unchanged)
    12. Resize window: desktop → mobile → desktop roundtrip
    13. No crashes, no console errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run lint` — ZERO warnings
- [ ] `npx tsc --noEmit` — ZERO errors
- [ ] `npm test -- --run` — ALL unit tests pass
- [ ] `npm run build` — succeeds
- [ ] `npx playwright test` — ALL E2E tests pass
- [ ] Mobile: bottom tabs + swipeable cards + kebab menu + pilot picker
- [ ] Desktop: header nav + grid + context menu (unchanged)
- [ ] Responsive transitions work without crash
- [ ] Phase 15: Mobile Responsive Nav COMPLETE
</verification>

<success_criteria>

- All quality gate checks pass
- All E2E tests pass (smoke + responsive-panels + mobile-responsive)
- Mobile experience works as described in CONTEXT.md
- Desktop experience unchanged
- Phase 15 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-responsive-nav/15-05-SUMMARY.md`
</output>
