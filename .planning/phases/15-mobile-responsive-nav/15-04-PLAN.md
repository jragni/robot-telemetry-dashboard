---
phase: 15-mobile-responsive-nav
plan: 04
type: execute
---

<objective>
Add Pilot tab route with robot picker + hide bottom bar during pilot mode.

Purpose: The Pilot tab needs a landing page (robot picker) and the bottom bar must hide during fullscreen pilot mode. Make failing pilot E2E tests pass (TDD GREEN).
Output: /pilot route with robot picker, bottom bar hidden during pilot mode.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-mobile-responsive-nav/15-CONTEXT.md
@.planning/phases/15-mobile-responsive-nav/15-RESEARCH.md
@.planning/phases/15-mobile-responsive-nav/15-03-SUMMARY.md

# Key source files:

@src/router/index.tsx
@src/views/PilotView.tsx
@src/components/layout/DashboardShell.tsx
@src/stores/connections/connections.store.ts
@e2e/mobile-responsive.spec.ts

**From CONTEXT.md:**

- Pilot tab → robot picker screen (list connected robots)
- If no robots → message + "add a robot" link
- Launch → fullscreen pilot (existing PilotMobileLayout)
- Bottom bar hides during pilot mode
- Exit pilot via [X] → returns to previous view

**From RESEARCH.md — Pitfall 3:**

- `/pilot/:robotId` already exists. Need a new `/pilot` route (no param) for the picker.
- PilotView currently redirects to /dashboard if no robotId — update to show picker instead.

**TDD target:** Make "Pilot Tab" E2E tests pass (un-fixme them first).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RobotPickerView + add /pilot route</name>
  <files>
    src/views/RobotPickerView.tsx (new),
    src/views/RobotPickerView.types.ts (new),
    src/router/index.tsx
  </files>
  <action>
**RobotPickerView.tsx:**
- Simple view listing connected robots from `useConnectionsStore`
- Each robot shows: name, base URL, connection status, "Launch Pilot" button
- Launch button navigates to `/pilot/{robotId}`
- Empty state: "No robots connected" message with suggestion to add one
- Title: "Select Robot" in defense-contractor mono style
- Renders inside DashboardShell (has bottom bar)

**router/index.tsx:**

1. Add a new route: `{ path: '/pilot', element: <RobotPickerView /> }` inside the DashboardShell children
2. Keep existing: `{ path: '/pilot/:robotId', element: <PilotView /> }` (outside DashboardShell — fullscreen)
3. The order matters: `/pilot/:robotId` must be checked before `/pilot` — or use separate route groups

**Update PilotView.tsx:**

- Remove the redirect to /dashboard when no robotId — that's now handled by the /pilot route showing the picker
- PilotView only renders when robotId is present (router guarantees this with `:robotId` param)
  </action>
  <verify>npm run lint && npx tsc --noEmit && npm test -- --run</verify>
  <done>RobotPickerView created. /pilot route added to router. Pilot tab navigates to picker.</done>
  </task>

<task type="auto">
  <name>Task 2: Hide bottom bar during pilot mode + un-fixme pilot E2E tests</name>
  <files>
    src/components/layout/DashboardShell.tsx,
    e2e/mobile-responsive.spec.ts
  </files>
  <action>
**DashboardShell.tsx:**
The bottom bar is already hidden for pilot mode if `/pilot/:robotId` is outside the DashboardShell route group (it renders PilotView directly, not through DashboardShell). Verify this is the case.

If needed: add a check in DashboardShell to hide BottomTabBar when route matches `/pilot/`:

```tsx
const isPilotMode = location.pathname.includes('/pilot/');
<Show when={isMobile && !isPilotMode}>
  <BottomTabBar />
</Show>;
```

**E2E tests:**

1. Un-fixme the pilot tab tests in `e2e/mobile-responsive.spec.ts`
2. Change `test.fixme(...)` to `test(...)` for the pilot picker tests
3. Run the full mobile-responsive E2E suite

```bash
npx playwright test e2e/mobile-responsive.spec.ts --reporter=list
```

ALL tests should pass now.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npx playwright test e2e/mobile-responsive.spec.ts --reporter=list</verify>
<done>Bottom bar hidden in pilot mode. All pilot E2E tests pass. Full mobile-responsive suite GREEN.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` — ZERO warnings
- [ ] `npx tsc --noEmit` — ZERO errors
- [ ] `npm test -- --run` — ALL unit tests pass
- [ ] ALL mobile-responsive E2E tests pass (no fixme remaining)
- [ ] All existing E2E tests still pass
- [ ] /pilot route shows robot picker
- [ ] /pilot/:robotId shows fullscreen pilot (no bottom bar)
</verification>

<success_criteria>

- RobotPickerView created with robot list + empty state
- /pilot route works in bottom tab bar
- Bottom bar hidden during fullscreen pilot mode
- All pilot E2E tests pass (TDD GREEN)
- All mobile-responsive E2E tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-responsive-nav/15-04-SUMMARY.md`
</output>
