---
phase: 15-mobile-responsive-nav
plan: 03
type: execute
---

<objective>
Create MobilePanelCarousel + PanelKebabMenu, wire into PanelGrid for mobile layout.

Purpose: Replace the react-grid-layout grid with a swipeable carousel on mobile. Add kebab menu for panel actions on touch devices. Make failing E2E tests for swipe and responsive transitions pass (TDD GREEN).
Output: Swipeable full-height panel cards on mobile with dot indicators and kebab menus. Grid on desktop unchanged.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/15-mobile-responsive-nav/15-CONTEXT.md
@.planning/phases/15-mobile-responsive-nav/15-RESEARCH.md
@.planning/phases/15-mobile-responsive-nav/15-02-SUMMARY.md

# Key source files:

@src/features/panels/components/PanelGrid.tsx
@src/features/panels/components/PanelFrame.tsx
@src/features/panels/components/PanelContextMenu.tsx
@src/components/shared/DataCard.tsx
@src/hooks/use-mobile.ts
@src/components/ui/carousel.tsx
@src/components/ui/dropdown-menu.tsx
@e2e/mobile-responsive.spec.ts

**From RESEARCH.md:**

- Conditional render: `if (isMobile) return <MobilePanelCarousel />` in PanelGrid
- shadcn Carousel with `setApi` for dot indicators via `selectedScrollSnap()`
- shadcn DropdownMenu with MoreVertical icon trigger for kebab
- `h-full` chain on Carousel → CarouselContent → CarouselItem for full-height cards
- Pitfall: bottom bar overlap — need `pb-14` on carousel container
- Pitfall: kebab button vs carousel swipe — header is outside touch-action zone

**From CONTEXT.md:**

- Swipeable cards on Dashboard + Map only (Fleet keeps its layout)
- Each card fills full available height
- Dot indicators: primary for active, muted for inactive
- Drag/resize disabled on mobile
- Kebab ⋮ button per panel replaces right-click context menu

**TDD target:** Make "Swipeable Panel Cards" and "Responsive Transitions" E2E tests pass.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MobilePanelCarousel + PanelKebabMenu components</name>
  <files>
    src/features/panels/components/MobilePanelCarousel.tsx (new),
    src/features/panels/components/MobilePanelCarousel.types.ts (new),
    src/features/panels/components/PanelKebabMenu.tsx (new),
    src/features/panels/components/PanelKebabMenu.types.ts (new)
  </files>
  <action>
**MobilePanelCarousel.types.ts:**
```typescript
import type { ViewId } from '@/features/panels/panel.types';

export interface MobilePanelCarouselProps {
viewId: ViewId;
robotId?: string;
}

````

**MobilePanelCarousel.tsx:**
- Read panels from `useLayoutStore((s) => s.getViewLayout(viewId))`
- Use shadcn `Carousel` with `setApi` to track selected index
- Each panel renders in a `CarouselItem` using `PanelFrame`
- Full-height: Carousel, CarouselContent, CarouselItem all get `h-full`
- Dot indicators below carousel: map `api.scrollSnapList()`, highlight `api.selectedScrollSnap()`
- Add `data-testid="mobile-carousel"` to the carousel container
- Add `data-testid="carousel-dots"` to the dots container
- No prev/next buttons (swipe only on mobile)
- Panel management (add/remove/reset) via PanelKebabMenu in each panel header

**PanelKebabMenu.types.ts:**
```typescript
import type { ViewId } from '@/features/panels/panel.types';

export interface PanelKebabMenuProps {
  viewId: ViewId;
  panelId?: string;
  onResetLayout: () => void;
}
````

**PanelKebabMenu.tsx:**

- MoreVertical icon (14px) as DropdownMenuTrigger
- Same menu items as PanelContextMenu: Add Panel (disabled in carousel for now), Duplicate (disabled), Remove, Reset Layout
- `panel-action-button` class on trigger to prevent drag conflicts
- Use shadcn DropdownMenu components
  </action>
  <verify>npm run lint && npx tsc --noEmit</verify>
  <done>MobilePanelCarousel and PanelKebabMenu created. Types in .types.ts files. Lint clean.</done>
  </task>

<task type="auto">
  <name>Task 2: Wire MobilePanelCarousel into PanelGrid + update PanelFrame for mobile</name>
  <files>
    src/features/panels/components/PanelGrid.tsx,
    src/features/panels/components/PanelFrame.tsx,
    src/components/shared/DataCard.tsx
  </files>
  <action>
**PanelGrid.tsx:**
1. Import `useMobile` and `MobilePanelCarousel`
2. Add `const isMobile = useMobile()`
3. At the top of the return, before the desktop grid:
```tsx
if (isMobile) {
  return (
    <MobilePanelCarousel viewId={viewId} robotId={robotId} />
  );
}
```
4. Desktop grid code stays unchanged below the mobile early return

**PanelFrame.tsx:**

1. Import `useMobile` and `PanelKebabMenu`
2. On mobile: show PanelKebabMenu ⋮ button in header (instead of hover × button)
3. On desktop: keep existing behavior (hover × button + context menu)
4. Use `<Show when={isMobile}>` for kebab, `<Show when={!isMobile}>` for × button

**DataCard.tsx:**

1. On mobile: remove `cursor-grab` from header (no drag on mobile)
2. Remove `panel-drag-handle` class on mobile (prevents accidental drag attempts)
3. Use `useMobile()` to conditionally apply these classes

**Run E2E tests:**

```bash
npx playwright test e2e/mobile-responsive.spec.ts --grep "Swipeable|Responsive"
```

These tests should NOW PASS.
</action>
<verify>npm run lint && npx tsc --noEmit && npm test -- --run && npx playwright test e2e/mobile-responsive.spec.ts --grep "Swipeable|Responsive" --reporter=list</verify>
<done>Mobile carousel renders on mobile, grid on desktop. Kebab menu on mobile panels. Swipe + responsive E2E tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Mobile responsive panel layout: bottom tab bar, swipeable panel cards with dot indicators, kebab menu per panel, grid on desktop unchanged. Full TDD — tests were written first.</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Open Chrome DevTools → Toggle device toolbar → iPhone 14 (390×844)
    3. Visit: /dashboard
    4. Verify: Bottom tab bar with 4 tabs (Dashboard, Fleet, Map, Pilot)
    5. Verify: Dashboard tab is highlighted (primary color)
    6. Verify: Panel cards are full-height, swipeable left/right
    7. Verify: Dot indicators below cards update on swipe
    8. Verify: ⋮ kebab button in panel header opens dropdown menu
    9. Tap Fleet tab → verify navigation works
    10. Tap Map tab → verify map panels in carousel
    11. Switch to Desktop viewport (1440×900)
    12. Verify: Bottom bar disappears, header nav returns, grid layout
    13. No console errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run lint` — ZERO warnings
- [ ] `npx tsc --noEmit` — ZERO errors
- [ ] `npm test -- --run` — ALL unit tests pass
- [ ] Swipeable Panel Cards E2E tests pass
- [ ] Responsive Transition E2E tests pass
- [ ] All existing E2E tests still pass
- [ ] Mobile: carousel with dots, kebab menu
- [ ] Desktop: grid with context menu (unchanged)
</verification>

<success_criteria>

- MobilePanelCarousel with swipeable full-height cards + dot indicators
- PanelKebabMenu with ⋮ dropdown on mobile panels
- PanelGrid conditionally renders carousel vs grid
- Swipe + Responsive E2E tests pass (TDD GREEN)
- Desktop behavior unchanged
  </success_criteria>

<output>
After completion, create `.planning/phases/15-mobile-responsive-nav/15-03-SUMMARY.md`
</output>
