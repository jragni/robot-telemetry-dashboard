export const MINIMAP_SIZE = 200;
// Fixed scale for the minimap — no zoom controls. 30 px/m gives a wider field
// of view than the default 50 px/m, which is better for the small canvas.
export const MINIMAP_SCALE = 30;
